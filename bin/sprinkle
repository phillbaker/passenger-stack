#!/usr/bin/env ruby
#
# This file was generated by RubyGems.
#
# The application 'sprinkle' is installed as part of a gem, and
# this file is here to facilitate running it.
#

require 'rubygems'
require 'bundler/setup'

version = ">= 0"

if ARGV.first =~ /^_(.*)_$/ and Gem::Version.correct? $1 then
  version = $1
  ARGV.shift
end

gem 'sprinkle', version
load Gem.bin_path('sprinkle', 'sprinkle', version)
